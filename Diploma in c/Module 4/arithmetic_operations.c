#include<stdio.h>

int main(void){

    //calculating the are of a square
    int length;
    printf("Enter the length of the square;\n");
    scanf("%d", &length);
    printf("The area of the square is:%d", length*length);
    return 0;
}